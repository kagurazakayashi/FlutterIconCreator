name: flutter_icon_creator
description: Command-line tool for generating icons for Flutter projects
version: 0.0.1
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.0
  path: ^1.9.1
  yaml: ^3.1.3

dev_dependencies:
  lints: ^3.0.0
